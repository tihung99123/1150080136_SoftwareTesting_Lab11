# Bai2 Standalone

This folder contains a standalone extract of Lab 9 Bài 2.

## Run

```bash
mvn test -Dbrowser=chrome
mvn test -Dbrowser=firefox
```

The suite runs from `testng.xml` and saves failure screenshots under `target/screenshots/`.
